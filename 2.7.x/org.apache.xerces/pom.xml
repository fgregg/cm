<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
xmlns="http://maven.apache.org/POM/4.0.0"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>choicemaker-cm</artifactId>
    <groupId>com.choicemaker.cm</groupId>
    <version>2.7.1-SNAPSHOT</version>
  </parent>
  <artifactId>org-apache-xerces</artifactId>
  <version>4.0.13</version>
  <packaging>pom</packaging>
  <name>choicemaker-cm3</name>
  <url>http://apache.org</url>
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <eclipse.antbuild.temp.folder>${project.build.directory}/temp.folder</eclipse.antbuild.temp.folder>
  </properties>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <phase>process-resources</phase>
            <!-- phase>compile</phase -->
            <configuration>
              <tasks>
              <!-- target -->
                <ant antfile="${basedir}/build.xml" target="zip.plugin" />
              <!-- /target -->
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>1.3</version>
        <executions>
          <execution>
            <id>add-jar</id>
            <phase>package</phase>
            <goals>
              <goal>attach-artifact</goal>
            </goals>
            <configuration>
              <artifacts>
                <artifact>
                  <file>
                  ${basedir}/xmlParserAPIs.jar</file>
                  <!-- type>jar</type -->
                  <classifier>optional</classifier>
                </artifact>
                <artifact>
                  <file>
                  ${basedir}/xercesImpl.jar</file>
                  <!-- type>jar</type -->
                  <classifier>optional</classifier>
                </artifact>
              </artifacts>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <dependencies/>
</project>
